# 바뀐 것

## 0.1.8

**Dev 모드가 데스크톱 앱을 앱으로 띄웁니다.** 전에는 `dev` → `start` → `serve` 순으로만 찾아서, Electron·Tauri 프로젝트의 `dev` 가 vite 만 띄우는 경우 개발 서버만 돌고 정작 앱 창은 뜨지 않았습니다. 이제 스크립트의 **이름이 아니라 본문이 무엇을 부르는지**로 고릅니다 — 이름은 프로젝트마다 제각각이지만(`electron:dev` · `dev:electron` · `tauri:dev` · `dev:start`) 본문에는 반드시 electron 이나 tauri 가 나옵니다. `"dev": "node scripts/dev.mjs"` 처럼 파일로 감싼 것은 그 파일을 한 겹 열어 봅니다. Tauri 기본 스캐폴드(`"tauri": "tauri"` + `src-tauri/`)는 `npm run tauri dev` 로 띄웁니다. 앱이 스스로 창을 여는 프로젝트는 포트를 기다리지도, 웹뷰 미리보기를 열지도 않습니다 — 그 미리보기 안에는 앱이 쓰는 네이티브 API 가 없어 기능 대부분이 죽습니다. 실행 화면의 `어디에 열까요?` 선택지도 그때는 숨깁니다.

**새 PC 에서 `Unknown command: /buildplanner:plan` 이 뜨던 것.** 슬래시 명령은 지시서가 `~/.claude/skills` 에 있어야 생기는데, 확장은 그걸 확인하지 않고 무조건 슬래시 명령을 적어 넣었습니다. 그러면 Enter 를 눌러도 오류 한 줄로 끝나고 단계 표시만 계속 돕니다. 이제 지시서가 그 자리에 실제로 있는지 보고, 없으면 `이 파일을 읽고 그대로 따라라` 로 바꿔 보냅니다 — Codex·Gemini 에게 쓰던 길과 같습니다. **이번에 켜면서 방금 깔았다면** 그 판에서도 파일로 넘깁니다. Claude 는 대화를 시작할 때 지시서 목록을 한 번 읽으므로, 이미 떠 있는 창은 방금 깔린 것을 모릅니다.

**로그인해 뒀는데도 "설치 안 됨" 으로 보이던 것.** 세 가지가 겹쳐 있었습니다.

- **한 번 아니면 그 판에서는 영영 아니었습니다.** 판정 결과를 통째로 기억해서, 터미널에서 깔고 로그인한 뒤 `AI 모델 고르기` 를 다시 열어도 계속 "설치 안 됨" 이었습니다. 이제 **찾은 것만** 기억합니다. 없는 명령을 다시 물어보는 값은 싸고(26ms), 찾은 것을 다시 물어보는 쪽이 비싸서(Gemini CLI 1.5초) 화면이 멈춥니다.
- **PATH 를 탔습니다.** 확장이 쓰는 PATH 는 VS Code 가 뜰 때 물려받은 것이라, `npm install -g` 가 PATH 를 바꿔도 이미 떠 있는 VS Code 는 모릅니다 — 터미널에서는 되는데 확장에서만 안 되는 자리입니다. 이제 PATH 가 모르면 `%APPDATA%\npm` 같은 흔한 자리를 직접 봅니다. 찾아낸 폴더는 실행 터미널의 PATH 앞에도 얹습니다. 터미널 역시 같은 PATH 를 물려받기 때문입니다.
- **모자란 것을 화면 밖에서 해결하게 했습니다.** `AI 모델 고르기` 에서 준비 안 된 것을 고르면 이제 `터미널에서 설치` · `터미널에서 로그인` 을 그 자리에서 눌러 진행합니다. 안 깔린 것에 로그인을 권하지 않습니다.

## 0.1.7

**변경 기록이 요청 목록에서 변경 기록이 됐습니다.** 전에는 `대화에서 모으기` 가 대화창에 친 말을 한 글자도 안 바꾸고 그대로 늘어놨습니다. 그래서 목록이 대화 원문 더미가 되고, 무엇을 바꾸자는 것인지도, 실제로 무엇이 바뀌었는지도 그 파일만 봐서는 알 수 없었습니다.

**모으면 지시 한 문장으로 다듬습니다.** 모으는 즉시 친 말이 `docs/CHANGES.md` 에 들어가고, 이어서 **그 증상과 코드에서 확인한 원인을 합쳐** 한 문장으로 줄입니다 — `어떤 부분을 · 어떻게 되도록 · 어떤 식으로` 처리해달라. 사람이 친 말은 증상이고 어디를 만져야 하는지는 코드에 있는데, 둘을 따로 두면 반영할 때 그 조사를 처음부터 다시 하게 됩니다. 원인을 확인하지 못하면 `어떤 식으로` 는 비워 둡니다 — 지어내지 않습니다. 고쳐 달라는 요청이 아닌 잡담과 질문은 이때 `보류` 로 내려갑니다. 다듬기가 안 돌아도 친 말이 항목 줄에 그대로 남아 있어서 잃는 것은 없습니다.

**반영하면 무엇을 고쳤는지가 따라붙습니다.** `계획서에 반영` 이 항목을 `반영함` 으로 옮길 때, 코드에서 확인한 것을 고친 파일과 함께 `결과:` 로 적습니다. 짐작해서 적지 않고, 수치가 있으면 수치로 적습니다.

```markdown
- [x] `09-06 03:18` 타임라인 슬라이더는 끄는 동안 화면이 한 프레임 안에 따라오도록,
      이동마다 전체를 다시 그리는 seek 을 requestAnimationFrame 으로 묶어서 처리해주세요.
  - 결과: seek 을 rAF 로 묶어 끄는 동안 한 프레임 안에 따라온다 — 48ms → 9ms (renderer/player.js).
```

**계획서로 갈 때는 문장을 자리대로 가릅니다** — `어떻게 되도록` 은 사양이라 계획서로, `어떤 식으로` 는 방법이라 일지로 갑니다. 계획서에는 `타임라인 슬라이더를 끄는 동안 화면이 한 프레임 안에 따라온다` 가 남습니다. 방법이 바뀌어도 사양은 남습니다.

**친 말은 이 문장으로 덮어써집니다.** 요청 원문을 따로 남기지 않으므로, 다듬을 때 뜻을 바꾸지 않고 확실하지 않은 대목은 쓴 표현을 그대로 살립니다. 원문이 필요하면 `git diff` 나 대화 기록에서 다시 가져올 수 있습니다.

## 0.1.6

**다듬기 — 계획서를 만든 뒤에 고친 것을 계획서로 되돌립니다.** 개발 현황 창 아래에 칸이 하나 생겼습니다. 계획서의 단계를 다 끝낸 뒤에도 대화창에서 시키는 수정과 추가는 계속 나오는데, 그 결과가 계획서로 돌아가지 않아 계획서가 "처음에 이렇게 만들려 했다" 에 멈춰 있었습니다.

**`대화에서 모으기`** — 대화창에서 시킨 요청을 모아 `docs/CHANGES.md` 에 적습니다. 창을 닫아도, 대화를 지워도 기록은 디스크에 남아 있어서 지난 것까지 가져옵니다. 도구 결과·시스템 알림·`진행해주세요` 같은 줄은 걸러냅니다.

**`계획서에 반영`** — 추린 항목을 세 문서로 나눠 보냅니다. 계획서에는 **사양**을, 개발 일지에는 **경위와 실패한 시도**를, 개발 현황에는 **한 일과 못 한 일**을. 이 구분이 없으면 계획서가 일지가 되고, 다른 프로젝트에서 꺼내 쓸 수 없게 됩니다. 못 고친 것은 `done: false` 로 남깁니다 — 지우면 없던 일이 됩니다.

**누르기 전까지 문서는 손대지 않습니다.** 체크로 뺀 항목은 `보류` 로 옮겨 두고 다음에 다시 올리지 않습니다. 목업 검토 창과 같은 순서입니다.

**지시서가 하나씩 깔립니다.** 전에는 `~/.claude/skills/buildplanner` 폴더가 있으면 통째로 건너뛰어서, 판을 올려 지시서를 새로 넣어도 이미 쓰던 사람에게는 영영 가지 않았습니다. 이제 없는 것만 넣습니다 — 고쳐 쓴 것은 그대로 둡니다.

## 0.1.4

**정해진 순서** — 계획 → 화면 목업 → 계획서에 반영 → 개발 현황 → 만들기. 계획이 끝나면 `화면 목업 만들기` 가 기본 버튼이 되고, 다음 순서가 화면에 적힙니다.

**개발 현황 문서 자동 작성** — 만들기가 시작될 때 `docs/PROGRESS.json` 이 없으면 그것부터 만들고 코드를 씁니다. 목업을 반영한 뒤든, `목업 없이 만들기` 를 누른 뒤든 같습니다. 파일이 생기면 개발 현황 창이 스스로 열립니다. 전에는 사람이 빈 화면에서 `현황 파일 만들어달라고 하기` 를 눌러야만 생겼습니다.

**명령 팔레트의 `화면 목업 만들기`** — 여기서 만든 목업도 반영하면 만들기로 이어집니다. 시작 화면에서 누른 것만 이어지고 팔레트에서 누른 것은 알림 하나로 끝났습니다.

## 0.1.2

화면을 다루는 자리들이 한 창에서 끝나도록 바뀌었습니다.

**목업 확인 창** — 무엇을 그릴지 적힌 글이 창 하나로 뜹니다. 예전에는 글은 편집기에, 단추는 우측 하단 알림에 있어서 아무 일도 일어나지 않은 것처럼 보였습니다.

**목업 검토 창** — 돌아온 그림 옆에 고칠 말을 적는 자리가 생겼습니다. 적어 준 말은 다음 프롬프트 뒤에 붙습니다. 마음에 안 드는 이유를 전할 방법이 "버리고 다시 만들기" 뿐이라 같은 그림을 다시 받곤 했습니다.

**목업 다시 보기** (`BUILD STUDIO: 목업 다시 보기`) — 만들어 둔 목업을 다시 열어 반영하거나 고칩니다. 창을 한 번 닫으면 그림이 남아 있어도 처음부터 새로 만들어야 했습니다.

**개발 현황** (`BUILD STUDIO: 개발 현황`) — 계획서의 단계를 늘어놓고, 아직 안 끝난 단계마다 진행 버튼을 답니다. `docs/PROGRESS.json` 을 읽고, 파일이 바뀌면 스스로 다시 그립니다.

**모드 토글** — 진행 버튼 옆에 `하나씩 확인` / `자동으로`. `자동으로` 는 파일 수정을 되묻지 않습니다 (`buildstudio.autoRun`). Shift+Tab 으로만 바꿀 수 있어 찾을 수 없던 것을 눌러야 할 자리 옆으로 꺼냈습니다.

**진행 화면** — 일이 끝나면 위쪽에 흐르던 선도 멈춥니다. 결과는 나왔는데 선만 계속 돌면 아직 일하는 중으로 읽혔습니다.

## 0.1.1

마켓플레이스 게시 준비 — 영문 안내와 설치 방법. 동작은 0.1.0 과 같습니다.

## 0.1.0

첫 공개. 계획 · 역설계 · 화면 목업 · 만들기 · 실행 · 배포 · GitHub 를 한 화면에서.
